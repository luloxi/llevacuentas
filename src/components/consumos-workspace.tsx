"use client";

import { TransactionsTable } from "@/components/transactions-table";

/** Consumos: toolbar + table + modals live inside TransactionsTable. */
export function ConsumosWorkspace() {
  return <TransactionsTable />;
}
